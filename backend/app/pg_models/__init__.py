"""
PostgreSQL models for the Elevate CRM (Phase 1 core).

These models are intentionally separate from `app.models/*` (legacy/partial work),
so we can migrate incrementally without breaking the existing Mongo implementation.
"""

